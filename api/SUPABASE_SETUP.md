// Supabase table definition for users
// Table: users
// Columns:
// - id: integer, primary key, auto-increment
// - username: text, unique
// - password: text
// - balance: integer (optional, default 0)

// You must create this table in your Supabase project for the API to work.
// Go to Supabase dashboard > Table Editor > Create table 'users' with columns above.

// .env file example:
// SUPABASE_URL=https://your-project.supabase.co
// SUPABASE_KEY=your-anon-key

// Add .env file to your project root and restart server after updating keys.